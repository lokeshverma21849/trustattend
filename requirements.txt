# TrustAttend — Python dependencies
# Install: pip install -r requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
bcrypt==4.1.3
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# Render needs this to detect Python version (optional but recommended)
# Create a file called  .python-version  containing just:  3.11.0
